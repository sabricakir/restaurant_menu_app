<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New food</h1>

  <%= turbo_frame_tag "new_food" do%>
    <%= render "form", user: current_user, restaurant: @restaurant , food: @food %>
  <%end%>
</div>
